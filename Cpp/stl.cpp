#include <bits/stdc++.h>
#include <array>
#include <deque>
#include <list>
using namespace std;

void print() {
  cout << "janak";
}

int sum(int a, int b) {
  return a + b;
}

int main() {
  // print();
  // int s = sum(1, 5);
  // cout << s;
  // return 0;

  // int basic[3] = {1, 3, 4};
  // array<int, 4> a = {2, 4, 5, 6};
  // int size = a.size();

  // cout << a.at(1) << endl;
  // cout << a.front() << endl;
  // cout << a.back() << endl;
  // cout << a.empty() << endl;

  // vector<int> v;
  // cout<<v.capacity()<<endl;
  // v.push_back(1);
  // cout<<v.capacity()<<endl;
  // v.push_back(3);
  // cout<<v.capacity()<<endl;
  // v.push_back(3);
  // cout<<v.capacity()<<endl;
  // v.push_back(5);
  // cout<<v.capacity()<<endl;
  // v.push_back(5);
  // cout<<v.capacity()<<endl;
  // // its double the size when its need the extra sizes
  // // the size is different
  // cout<<v.size()<<endl;

  // vector<int> vec = {1, 2, 3, 4, 5};

  // // cout<<vec.begin()<<endl;
  // // cout<<vec.end()<<endl;

  // // Using for_each and a lambda function to print the vector
  // for_each(vec.begin(), vec.end(), [](int n) { cout << n << ' '; });
  // cout << endl;

  // cout<<vec.capacity()<<endl;
  // vec.clear();
  // for_each(vec.begin(), vec.end(), [](int n) { cout << n << ' '; });
  // cout << endl;
  // cout<<vec.capacity()<<endl;

  // vector<int> vecA(5, 2);
  // vector<int> vecB(vecA);

  // for_each(vecB.begin(), vecB.end(), [](int n) { cout << n << ' '; });
  // cout << endl;

  // deque<int> deq;

  // deq.push_back(32);
  // deq.push_front(54);
  // deq.push_front(67);
  // deq.push_back(56);

  // copy(deq.begin(), deq.end(), ostream_iterator<int>(cout, " "));
  // cout << endl;

  // deq.pop_back();
  // copy(deq.begin(), deq.end(), ostream_iterator<int>(cout, " "));
  // cout << endl;

  // deq.pop_front();
  // copy(deq.begin(), deq.end(), ostream_iterator<int>(cout, " "));
  // cout << endl;

  // cout<<deq.at(1)<<endl;

  // cout<<deq.size()<<endl;
  // deq.erase(deq.begin(), deq.begin()+1);
  // cout<<deq.size()<<endl;

  list<int> lis;

  





  return 0;
}
